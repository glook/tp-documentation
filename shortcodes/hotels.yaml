paths:
  tp_hotels_selections_discount_shortcodes:
    get:
      description: 'Получаем данные'
      tags: &hotelTags
        - 'Таблицы - Отели'
      responses:
        200:
          description: 'Отображение аттрибутов модели'
          schema:
            $ref: '#/models/tp_hotels_selections_discount_shortcodes'
    put: &putResponse
      tags: *hotelTags
      description: 'Сохраняем данные'
      responses:
        200:
          description: 'Сохранение данных произошло успешно'
          schema:
            $ref: '../response.yaml#/ApiResponse'
        400:
          description: 'Ошибка сохранения данных'
          schema:
            $ref: '../response.yaml#/ApiResponseError'
    delete:
      tags: *hotelTags
      description: 'Сброс данных до дефолтных состояний'
      responses:
        200:
          description: 'Возвращаем дефолтные настройки'
          schema:
            $ref: '#/models/tp_hotels_selections_discount_shortcodes'
  tp_hotels_selections_date_shortcodes:
    get:
      description: 'Получаем данные'
      tags: *hotelTags
      responses:
        200:
          description: 'Отображение аттрибутов модели'
          schema:
            $ref: '#/models/tp_hotels_selections_date_shortcodes'
    put: *putResponse
    delete:
      tags: *hotelTags
      description: 'Сброс данных до дефолтных состояний'
      responses:
        200:
          description: 'Возвращаем дефолтные настройки'
          schema:
            $ref: '#/models/tp_hotels_selections_date_shortcodes'
models:
  tp_hotels_selections_discount_shortcodes:
    type: 'object'
    description: 'Hotels collection - Discounts
<br/>
Подборка отелей со скидками по локации
<br/>
Работа с апи:
<br/>
<br/>
https://bitbucket.org/travelpayouts/wp-plugin-new/src/5e8e2b45eba6462c8035d41e5c4c8e88e672bd0b/includes/request-api/class-tp-request-api-hotel.php#class-tp-request-api-hotel.php-103'
    properties:
      title:
        $ref: '../variables.yaml#/$headerText'
      title_tag:
        $ref: '../variables.yaml#/$title_tags'
      columns:
        $ref: '../variables.yaml#/$shortcode_columns'
      button_text:
        $ref: '../variables.yaml#/$button_text'
      assign_dates:
        type: boolean
        description: 'Подставлять даты при переходе'
        format: 'checkbox'
      sort_by:
        allOf:
          - $ref: '../variables.yaml#/$sort_by'
          - enum:
              - 'Старая и новая цена'
              - 'Дата возвращения'
              - 'Пересадки'
              - 'Номер рейса'
              - 'Рейс'
              - 'Стоимость'
      use_pagination:
        $ref: '../variables.yaml#/$use_pagination'
      pagination_size:
        $ref: '../variables.yaml#/$pagination_size'
      additionalMarker:
        $ref: '../variables.yaml#/$additionalMarker'
      apply_to_all_tables:
        $ref: '../variables.yaml#/$apply_to_all_tables'
  tp_hotels_selections_date_shortcodes:
    type: 'object'
    description: 'Hotels collections for dates
<br/>
Подборки отелей на даты
<br/>
Работа с апи:
<br/>
<br/>
https://bitbucket.org/travelpayouts/wp-plugin-new/src/5e8e2b45eba6462c8035d41e5c4c8e88e672bd0b/includes/request-api/class-tp-request-api-hotel.php#class-tp-request-api-hotel.php-143'
    properties:
      title:
        $ref: '../variables.yaml#/$headerText'
      title_tag:
        $ref: '../variables.yaml#/$title_tags'
      columns:
        allOf:
          - $ref: '../variables.yaml#/$shortcode_columns'
          - enum:
              - 'До центра'
              - 'Отель'
              - 'Звездность'
              - 'Рейтинг'
              - 'Цена за ночь'
              - 'Кнопка'
      button_text:
        $ref: '../variables.yaml#/$button_text'
      sort_by:
        allOf:
          - $ref: '../variables.yaml#/$sort_by'
          - enum:
              - 'Старая и новая цена'
              - 'Дата возвращения'
              - 'Пересадки'
              - 'Номер рейса'
              - 'Рейс'
              - 'Стоимость'
      use_pagination:
        $ref: '../variables.yaml#/$use_pagination'
      pagination_size:
        $ref: '../variables.yaml#/$pagination_size'
      additionalMarker:
        $ref: '../variables.yaml#/$additionalMarker'
      apply_to_all_tables:
        $ref: '../variables.yaml#/$apply_to_all_tables'
